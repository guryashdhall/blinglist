import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    banner: {
      margin: "20px auto",
      //minWidth: 960,
      textAlign: 'center',
    },
    banner_img: {
      width: "70%",
      height: 480,
      cursor: "pointer",
    }
  }));